import styles from "../../../styles/comps/transHistory.module.css"

const TransferHistory = (props) => {
    return (<div className={styles.transferHistory}>
        <h1>No current transfer history available</h1>
    </div>)
}

export default TransferHistory